{
  "name": "otpless-js-sdk",
  "version": "1.0.0",
  "description": "SDK for authenticating users using WhatsApp",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx babel src --out-dir dist",
    "start": "npx babel src --watch --out-dir dist"
  },
  "keywords": [
    "otpless",
    "SDK",
    "javascript",
    "authentication"
  ],
  "author": "ashwin@otpless.com",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.18.10",
    "@babel/core": "^7.19.1",
    "@babel/preset-env": "^7.19.1"
  }
}
